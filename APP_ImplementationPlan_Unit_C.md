# APP_ImplementationPlan_Unit_C: 부분적 상태 주입 (상세 실행 계획)

본 계획은 불필요한 Props 전달(Prop Drilling)을 줄이기 위해 전역적으로 공유가 필요한 특정 상태들만 Context API로 전환하는 것을 목표로 합니다. 점진적인 전환과 엄격한 기록을 원칙으로 합니다.

## 📌 핵심 원칙
1. **디자인 유지**: 사용자 인터페이스(UI) 디자인은 절대로 수정하지 않습니다. 데이터의 흐름만 개선하며 화면에 보이는 모습은 동일합니다.
2. **MLX 전담**: Context 구조 설계 및 상태 이전 작업은 전적으로 로컬 MLX 모델을 사용합니다.
3. **상호 독립성**: UI 구조(Unit B)나 로직 분리(Unit A) 작업과 무관하게, 특정 데이터 스트림만 선택적으로 최적화할 수 있습니다.
4. **D2C 프롬프팅 적용**: MLX 추론 시 '생각 과정'을 생략하고 '코드 결과물'에 집중하는 구조화된 프롬프트를 사용하여 토큰 유실을 방지합니다.
5. **MLX 중심의 3단계 협업 고도화**: 
   - **1단계 (로컬 MLX)**: 컴포넌트 설계 및 Props 인터페이스 사전 정의, 안티패턴(Stale Closure, 리스너 중복 등) 정밀 분석, 국지적 코드 생성.
   - **2단계 (Cloud Gemini)**: MLX의 설계안을 기반으로 전역 시스템 의존성 검증, 최종 코드 조립 및 `App.jsx` 주입 연동.
   - **3단계 (사용자)**: MLX가 제안한 Edge Case 시나리오를 바탕으로 브라우저 UI 최종 QA 및 무결성 확정.

---

## 🛠 1단계: 인증 및 사용자 컨텍스트 도입 (AuthContext)
- **작업 내용**: 
  - `currentUser`, `isAuthorized` 상태를 `src/contexts/AuthContext.js`로 이동.
- **검증 계획**:
  - [ ] **[AI 코드 분석] Context Provider**: `AuthProvider`가 `AppWrapper`에 감싸져 모든 하위 컴포넌트에서 `useAuth()` 훅을 통해 상태를 참조할 수 있는지 확인.
  - [ ] **[브라우저 동작 확인] Session Persistence**: 로그아웃 후 다시 접속했을 때 세션 정보가 Context에 동기화되어 메인 대시보드가 정상적으로 뜨는지 확인.
- **수정 기록 지침**:
  - 파일 이름 규칙: `APP_StepLog_UnitC_Step1.md`
  - 내용: 수정된 함수의 라인 번호, 변경 전/후 로직 요약, 이슈 및 해결 방법 포함.
- **핵심 원칙 준수**: [ ] 디자인 유지, [ ] MLX 전담, [x] 독립성 보장

## 🛠 2단계: 프로젝트 전역 상태 주입 (ProjectContext)
- **작업 내용**: 
  - `activeProject`, `isArchived`, `lockReason` 등 워크스페이스 전역에서 참조하는 상태를 `ProjectContext`로 전환.
- **검증 계획**:
  - [ ] **[AI 코드 분석] Prop Drilling 제거**: `WorkspaceView`와 하위 탭 컴포넌트 사이의 Props 전달 코드가 삭제되고 `useProject()` 훅으로 교체되었는지 확인.
  - [ ] **[브라우저 동작 확인] 참조 무결성**: 다른 프로젝트로 전환 시, Context에 담긴 이전 프로젝트의 잠금 상태나 데이터가 완벽히 새 프로젝트의 것으로 교체되는지 확인.
- **수정 기록 지침**:
  - 파일 이름 규칙: `APP_StepLog_UnitC_Step2.md`
  - 내용: 수정된 함수의 라인 번호, 변경 전/후 로직 요약, 이슈 및 해결 방법 포함.
- **핵심 원칙 준수**: [ ] 디자인 유지, [ ] MLX 전담, [x] 독립성 보장

## 🛠 3단계: UI 및 폼 상태 통합 주입 (UIContext)
- **작업 내용**: 
  - `isFormDirty`, `dbUpdatedAt`, `onFormDirtyChange` 등 UI 반응성과 관련된 상태를 `UIContext`로 통합.
- **검증 계획**:
  - [ ] **[AI 코드 분석] 전역 반응성 확인**: 임의의 탭에서 데이터를 수정했을 때, Context를 통해 헤더의 "Unsaved" 배지가 즉시 갱신되는지 확인.
  - [ ] **[브라우저 동작 확인] 데이터 일관성**: 저장 완료 후 `dbUpdatedAt`이 Context를 통해 전파되어 모든 UI 요소의 타임스탬프가 동기화되는지 확인.
- **수정 기록 지침**:
  - 파일 이름 규칙: `APP_StepLog_UnitC_Step3.md`
- **핵심 원칙 준수**: [ ] 디자인 유지, [ ] MLX 전담, [x] 독립성 보장

---

## ✅ 최종 검증 체크리스트
- [ ] 모든 단계별 수정 기록 파일(`APP_StepLog_UnitC_StepN.md`)이 생성되었는가?
- [ ] Context 전환 후에도 UI 디자인이나 사용자 경험이 이전과 완벽히 동일한가?
- [ ] 코드의 가독성이 향상되고 Props 전달 계층이 단순해졌는가?

---

## 💡 로컬 모델(MLX-Qwen) 활용 가이드라인 및 레슨런
Unit B 작업 과정에서 도출된 로컬 추론형 모델(MLX-Qwen 27B 등)의 특징과 한계를 바탕으로, 본 프로젝트 전반에 적용할 프롬프팅 및 환경 설정 가이드라인입니다.

1. **프롬프팅 전략: 억압 대신 분리 유도 (Structured Output)**
   - **문제**: "생각하지 마(No thinking)", "JSON만 출력해"와 같은 강력한 부정어 제약(Negative Prompting)은 추론형 모델에서 쉽게 무시되며, 오히려 불필요한 텍스트 출력으로 이어집니다.
   - **해결**: 모델의 본능적인 '단계별 추론(Chain-of-Thought)'을 허용하되, `<think>생각 과정</think>`와 같이 별도 태그로 격리시키고 최종 결과물만 `<json>결과</json>` 등에 담도록 구조화된 출력을 요구해야 합니다.
2. **토큰 자원 할당: `max_tokens`의 여유로운 설정**
   - **문제**: 복잡한 코드 블록 분석 시, 모델이 추론을 작성하다가 `max_tokens`(예: 1200) 제한에 걸려 실제 결과물 출력이 중간에 잘리는 현상(Truncation)이 발생합니다.
   - **해결**: 코드 구조 분석이나 컴포넌트 Props 추출과 같은 고부하 태스크에서는 `max_tokens`를 최소 2000~4000 이상으로 넉넉하게 설정하여 모델이 작업을 끝마칠 수 있는 환경을 보장해야 합니다.
3. **태스크 세분화 (Single-Purpose Prompting)**
   - 한 번의 프롬프트로 "분석하고, 코드 짜고, JSON으로 포맷팅해"라고 요구하는 대신, "1. 상태만 추출해", "2. 함수만 추출해"처럼 로컬 모델의 처리 한계에 맞춰 지시를 잘게 쪼개는 것이 정확도를 높이는 지름길입니다.
